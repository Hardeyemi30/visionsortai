
# --- Edge / local processing ---
Pillow>=10.0
numpy>=1.24
scipy>=1.11          # scipy.ndimage.laplace for blur scoring
ImageHash>=4.3        # perceptual hashing for duplicate detection
python-dotenv>=1.0

# --- Cloud (AI agent + backup) ---
openai>=1.0            # Azure OpenAI client (AzureOpenAI class)
azure-storage-blob>=12.19
azure-cosmos>=4.5      # searchable index, shared schema with the teammate's api/ dashboard code

# --- Web interface ---
Flask>=3.0
qrcode[pil]>=7.4        # printable QR code generation
gunicorn                # production WSGI server for the Azure App Service deployment

# --- Testing ---
pytest>=8.0

# NOTE: Pi-only hardware dependencies (gpiozero, lgpio for the status box's
# bar graph) live in requirements-pi.txt, NOT here. lgpio compiles a native
# C extension against liblgpio and fails to build anywhere that system
# library isn't present -- including Azure's build server, which broke the
# "Build and deploy Python app to Azure Web App" GitHub Actions workflow
# when these were briefly listed in this file. Keep them out of here.